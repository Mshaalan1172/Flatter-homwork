//What is the difference between the num and double data types in Dart?

//num is used to define any number ( integer or double) but double is used to define only double numbers

void main() {
  num a = 10;
  print(a);
  num b = 10.21;
  print(b);
  double c = 12.01;
  print(c);
}
